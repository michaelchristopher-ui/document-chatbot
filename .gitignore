.env
.venv/
__pycache__/
*.pyc
*.pyo
.DS_Store
*.db
*.db.lock
# Where the dev compose overlay puts the container's state.
.data/
# The interaction log runs in WAL mode, which keeps two sidecars beside the file.
*.db-wal
*.db-shm
documents/*.pdf
