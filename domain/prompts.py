"""Moved: the prompts now live in `domain.constants`, with every other constant.

Kept as a pointer rather than removed, because `domain.prompts` is named in the
README and in several docstrings — the prompts themselves are unchanged.
"""
