.venv/
.git/
.env
__pycache__/
*.pyc
*.pyo
.DS_Store
.vscode/
# Local state. The deployed app keeps its own on a volume, and baking a
# developer's index into the image would ship their documents with it.
*.db
*.db-wal
*.db-shm
*.db.lock
documents/*.pdf
